#include "aws_mqtt_msg.h"

/**
 * @brief convert an activity msg struct into a string
 * 
 * @param msg activity msg struct
 * @param buff char buffer
 */
void activity_msg_to_string(activity_msg_t *msg, char *buff)
{
    ;
}

/**
 * @brief convert a pairing msg struct into a string
 * 
 * @param msg pairing msg struct
 * @param buff char buffer
 */
void pairing_msg_to_string(pairing_msg_t *msg, char *buff)
{
    ;
}