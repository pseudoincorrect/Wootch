// Config for the app
// a little bit redundant with cdk.json
// We will probably merge it into cdk.json in the future

// Cognito and Auth
export const usersGroupName = "usersGroup";
export const adminsGroupName = "adminsGroup";
export const authorizationHeaderName = "Authorization";
